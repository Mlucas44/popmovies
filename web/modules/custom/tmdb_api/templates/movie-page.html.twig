{{ attach_library('tmdb_theme/infinite_scroll') }}

<div class="container mx-auto px-4 py-12">
	<div class="mb-12 flex items-end justify-between border-b border-pop-toile pb-4">
		<div>
			<h1 class="text-3xl md:text-5xl font-black text-pop-blanc tracking-tight mb-2">{{ title }}</h1>
			<p class="text-pop-blanc/60 text-sm md:text-base">{{ intro_text }}</p>
		</div>
	</div>

	<div id="movies-grid" data-api-url="{{ api_url }}" data-current-page="1" class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-x-10 gap-y-8 md:gap-x-14 md:gap-y-12">
		{% for movie in movies %}
			{% include '@tmdb_api/movie-card.html.twig' with {'movie': movie} %}
		{% endfor %}
	</div>

	<div id="scroll-trigger" class="flex justify-center py-8">
		<div class="w-8 h-8 border-4 border-pop-or border-t-transparent rounded-full animate-spin"></div>
	</div>
</div>
